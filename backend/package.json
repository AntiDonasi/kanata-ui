{
  "name": "kanata-bot-backend",
  "version": "1.0.0",
  "description": "Backend API for Kanata Bot using Elysia.js",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "elysia": "^0.8.17",
    "@elysiajs/cors": "^0.8.0",
    "@elysiajs/swagger": "^0.8.5"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.3.3"
  }
}